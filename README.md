# Eisenhower Matrix

A lightweight, dependency-free Eisenhower Matrix built with plain HTML, CSS, and JavaScript.

Open `index.html` directly in a browser, or serve the folder with any static web server. Tasks are saved in the browser with `localStorage`. Add, edit, delete, search, filter, and drag tasks between the four quadrants or back to Inbox.
